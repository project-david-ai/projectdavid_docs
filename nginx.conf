server {
    listen       80;
    server_name  _;

    root  /usr/share/nginx/html;
    index index.html;

    # SPA fallback: if a file/dir isn’t found, serve index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
